import{syncItems}from"./lib/notion.mjs";import{getWereadItems}from"./sources/weread.mjs";import{getDoubanItems}from"./sources/douban.mjs";
const source=(process.env.SOURCE||"all").toLowerCase();await verifyLicense();const notionToken=required("NOTION_TOKEN"),databaseId=required("NOTION_DATABASE_ID");let items=[];
if(source==="all"||source==="weread")items.push(...await getWereadItems(required("WEREAD_API_KEY")));
if(source==="all"||source==="douban")items.push(...await getDoubanItems({userId:required("DOUBAN_USER_ID"),apiKey:required("DOUBAN_API_KEY"),authToken:process.env.DOUBAN_AUTH_TOKEN,apiHost:process.env.DOUBAN_API_HOST}));
console.log(`准备同步 ${items.length} 条 ${source} 内容`);const results=await syncItems(notionToken,databaseId,items),failed=results.filter(item=>!item.ok);console.log(`同步完成：成功 ${results.length-failed.length}，失败 ${failed.length}`);for(const item of failed)console.error(`- ${item.title}: ${item.error}`);if(failed.length)process.exitCode=1;
async function verifyLicense(){const base=required("LICENSE_API_BASE"),licenseKey=required("TUNNEST_LICENSE_KEY"),repository=process.env.GITHUB_REPOSITORY||"local",deviceId=process.env.LICENSE_DEVICE_ID||`${repository}:actions`;const response=await fetch(`${base.replace(/\/$/,"")}/v1/licenses/verify`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({licenseKey,deviceId,clientType:"github-actions",deviceLabel:repository,extensionVersion:"github-actions"})});const data=await response.json().catch(()=>({}));if(!response.ok||!data.valid)throw new Error(data.error||"需要有效付费许可证，GitHub Actions 不提供试用同步");console.log(`付费许可证有效：${data.license.plan}${data.license.expiresAt?`，到期 ${data.license.expiresAt}`:"，永久"}`)}
function required(name){const value=process.env[name]?.trim();if(!value)throw new Error(`缺少环境变量 ${name}`);return value;}
