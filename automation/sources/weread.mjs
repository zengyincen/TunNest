const GATEWAY="https://i.weread.qq.com/api/agent/gateway";
export async function getWereadItems(apiKey){if(!apiKey)throw new Error("缺少 WEREAD_API_KEY");const client=(apiName,params={})=>gateway(apiKey,apiName,params);const books=[];let lastSort;for(;;){const data=await client("/user/notebooks",{count:100,...(lastSort!==undefined?{lastSort}:{})});const batch=data.books||[];books.push(...batch);if(!data.hasMore||!batch.length)break;lastSort=batch[batch.length-1].sort;}const items=[];for(const entry of books){const book=entry.book||entry,bookId=String(book.bookId||"");if(!bookId)continue;const[marks,notes,chaptersData]=await Promise.all([client("/book/bookmarklist",{bookId}),allReviews(client,bookId),client("/book/chapterinfo",{bookId})]);const chapters=Object.fromEntries((chaptersData.chapters||[]).map(chapter=>[String(chapter.chapterUid),chapter.title||chapter.chapterTitle||""]));const bookmarks=marks.updated||[];const reviews=(notes.reviews||[]).map(value=>value.review||value);const highlights=[...bookmarks.map((mark,index)=>({externalId:String(mark.bookmarkId||`mark-${index}`),text:mark.markText||"",chapter:chapters[String(mark.chapterUid)]||"",position:index})),...reviews.map((note,index)=>({externalId:String(note.reviewId||`note-${index}`),text:note.abstract||note.content||"",note:note.type===1?note.content:"",chapter:chapters[String(note.chapterUid)]||"",position:bookmarks.length+index}))].filter(item=>item.text);items.push({source:"weread",kind:"book",externalId:bookId,title:book.title||book.bookName||"未命名书籍",author:book.author||"",url:`https://weread.qq.com/web/bookDetail/${bookId}`,excerpt:`${highlights.length} 条划线与笔记`,tags:["微信读书"],coverUrl:book.cover||book.coverUrl,highlights,capturedAt:new Date().toISOString()});}return items;}
async function allReviews(client,bookId){const reviews=[];let synckey=0;for(;;){const data=await client("/review/list/mine",{bookid:bookId,synckey,count:100});reviews.push(...(data.reviews||[]));if(!data.hasMore||!(data.reviews||[]).length)break;synckey=data.synckey||0;}return{reviews};}
async function gateway(apiKey,apiName,params){const response=await fetch(GATEWAY,{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({api_name:apiName,skill_version:"1.0.4",...params})});const data=await response.json().catch(()=>({}));if(!response.ok||data.errcode)throw new Error(data.errmsg||`微信读书 Gateway 失败：${apiName}`);if(data.upgrade_info)throw new Error(`微信读书 Gateway 需要升级：${data.upgrade_info}`);return data;}
