import{cpSync,mkdirSync,rmSync}from"node:fs";import{join}from"node:path";import{spawnSync}from"node:child_process";const root=new URL("..",import.meta.url).pathname,dist=join(root,"dist"),unpacked=join(dist,"tunnest-extension");rmSync(dist,{recursive:true,force:true});mkdirSync(dist,{recursive:true});cpSync(join(root,"extension"),unpacked,{recursive:true});const zip=spawnSync("zip",["-qr","tunnest-extension.zip","tunnest-extension"],{cwd:dist,stdio:"inherit"});if(zip.status!==0)throw new Error("zip 打包失败");console.log(`Extension package: ${join(dist,"tunnest-extension.zip")}`);
