import{writeFileSync}from"node:fs";const base=required("LICENSE_API_BASE").replace(/\/$/,""),response=await fetch(`${base}/v1/admin/licenses`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${required("LICENSE_ADMIN_TOKEN")}`},body:JSON.stringify({customerId:required("CUSTOMER_ID"),plan:required("PLAN"),note:process.env.NOTE||""})}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||`签发失败 (${response.status})`);console.log("::add-mask::"+data.licenseKey);writeFileSync("license.txt",`TunNest 许可证\n客户：${data.license.customerId}\n套餐：${data.license.plan}\n到期：${data.license.expiresAt||"永久"}\n许可证：${data.licenseKey}\n`,{mode:0o600});console.log(`许可证已签发：${data.license.id} · ${data.license.plan} · ${data.license.expiresAt||"永久"}`);function required(name){const value=process.env[name]?.trim();if(!value)throw new Error(`缺少 ${name}`);return value;}
